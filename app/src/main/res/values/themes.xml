<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme. -->
    <style name="Theme.MyFCM" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ic_receive_background</item>
        <item name="colorPrimaryVariant">@color/ic_receive_background_status</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/receive_background_status</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Toolbar back color -->
        <item name="colorControlNormal">@color/black_light_10</item>
    </style>

    <!-- Splash theme. -->
    <style name="Theme.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/ic_receive_background</item>
        <item name="colorPrimaryVariant">@color/ic_receive_background_status</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <!-- Customize your theme here. -->
    </style>
</resources>